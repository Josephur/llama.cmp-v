# Reconstructible source series

Apply these patches in manifest order to the recorded upstream base with `git apply` or `git am`. This series reproduces the inherited source changes. It does not certify performance or correctness; no benchmark verdict is implied by a patch appearing here.
