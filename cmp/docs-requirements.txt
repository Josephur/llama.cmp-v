# Documentation portal dependencies.
#
#   python3 -m venv cmp/.venv
#   cmp/.venv/bin/pip install -r cmp/docs-requirements.txt
#   cmp/.venv/bin/mkdocs serve -f cmp/mkdocs.yml --dev-addr 127.0.0.1:8321
#
# cmp/.venv is git-ignored by cmp/.gitignore. Pinned so a rebuild years from
# now renders the same site the workflow renders today.

mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-awesome-nav==3.3.0
pymdown-extensions==11.0.2
PyYAML==6.0.3
